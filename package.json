{
  "name": "dt-deployment-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd report-center/backend && npm install && npx playwright install chromium && npm run build",
    "start": "cd report-center/backend && npm start"
  }
}
