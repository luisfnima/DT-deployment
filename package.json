{
  "name": "dt-deployment-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root build handler for Render auto-deployments",
  "scripts": {
    "build": "cd report-center/backend && npm install && npm run build",
    "start": "cd report-center/backend && npm start"
  }
}
